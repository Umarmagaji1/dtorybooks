<h1>Welcome {{user.firstname}}</h1>
{{#if stories}}
<table class="striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Date</th>
            <th>Status</th>
            <th></th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        {{#each stories}}
        <tr>
            <td>{{title}}</td>
            <td>{{formatDate date 'MMMM do YYYY'}}</td>
            <td>{{status}}</td>
            <td><a class="btn grey darken-3" href="/stories/edit/{{id}}"><i class="fa fa-pencil"></i> Edit</a></td>
            <td>
                <form action="/stories/{{id}}?_method=DELETE" id="delete-form" method="POST"></i>
                    <button class="btn red darken-3" type="submit"><i class="fa fa-remove"></i> Delete</button>
                    <input type="hidden" name="_method" value="DELETE">
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<p>you have not stories.</p>
{{/if}}